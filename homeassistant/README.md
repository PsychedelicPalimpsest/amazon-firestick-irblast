# Legacy shell_command examples

Prefer the native integration in `../custom_components/firetv_ir` (Config Flow, media_player, remote).

These YAML files remain only for debugging `./ftvir` from automations.
